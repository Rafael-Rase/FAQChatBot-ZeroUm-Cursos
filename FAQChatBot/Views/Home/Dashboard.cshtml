@{
    Layout = null; // Desativa o layout padrão do ASP.NET.
}

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Zero Um Cursos</title>

    <!-- CSS -->
    <link rel="stylesheet" href="~/css/styles.css">
    <link rel="stylesheet" href="~/css/dashboard.css" />

    <style>

        /* Esconde o ícone do menu em telas grandes */
        #menu-toggle {
            display: none !important;
        }

        /* Exibe o botão hambúrguer apenas em telas pequenas (celulares) */
        @@media (max-width: 768px) {
            #menu-toggle {
                display: block !important;
                position: fixed;
                top: 20px;
                left: 20px;
                z-index: 1000;
                background: none;
                border: none;
                color: #9c45e8;
                font-size: 2rem;
                cursor: pointer;
            }
            /* Esconde a sidebar inicialmente no mobile */
            .sidebar {
                position: fixed;
                left: -250px;
                top: 0;
                height: 100%;
                width: 250px;
                background-color: #111;
                transition: left 0.3s ease;
                z-index: 999;
            }
                /* Quando o menu abre */
                .sidebar.open {
                    left: 0;
                }
            /* Empurra o conteúdo quando o menu abre */
            .main-content {
                padding-left: 20px;
            }
        }


        /* Container principal do dashboard */
        .dashboard-container {
            display: flex;
            min-height: 100vh;
            background-color: #000000;
        }

        /* Sidebar lateral */
        .sidebar {
            width: 250px;
            background-color: #111111;
            padding: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.5);
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

            .sidebar h2 {
                color: #9c45e8;
                margin-bottom: 30px;
                font-size: 1.5em;
            }

        .nav-link {
            display: block;
            width: 100%;
            padding: 12px 15px;
            margin-bottom: 10px;
            text-decoration: none;
            color: #aaaaaa;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
            font-weight: bold;
        }

            .nav-link:hover,
            .nav-link.active {
                background-color: #2a2a2a;
                color: #ffffff;
                border-left: 3px solid #9c45e8;
                padding-left: 12px;
            }

        .main-content {
            flex-grow: 1;
            padding: 40px;
            background-color: #0d0d0d;
        }

        .dashboard-content {
            background-color: #1a1a1a;
            padding: 40px;
            border-radius: 10px;
            text-align: center;
            width: 100%;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
        }

            .dashboard-content h1 {
                color: #9c45e8;
                font-size: 3em;
                margin-bottom: 5px;
                font-weight: 700;
                text-shadow: 0 0 5px rgba(156, 69, 232, 0.5);
            }

            .dashboard-content p {
                color: #ffffff;
                font-size: 1.4em;
                margin-bottom: 30px;
                font-weight: 300;
                line-height: 1.4;
            }

        .course-card {
            background-color: #2a2a2a;
            padding: 25px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: left;
            border-left: 5px solid #9c45e8;
            transition: transform 0.2s;
        }

            .course-card:hover {
                transform: translateY(-3px);
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            }

            .course-card h3 {
                color: #ffffff;
                margin-bottom: 8px;
                font-size: 1.5em;
            }

            .course-card p {
                color: #aaaaaa;
                font-size: 1em;
                margin-bottom: 0;
            }
    </style>
</head>

<body class="dashboard">

    <button id="menu-toggle">☰</button>

    <div class="dashboard-container">
        <div class="sidebar">
            <h2>Zero Um Cursos</h2>
            <a href="#" class="nav-link active">🎓 Meus Cursos</a>
            <a href="#" class="nav-link">📚 Biblioteca</a>
            <a href="#" class="nav-link">⚙ Configurações</a>
            <a href="#" class="nav-link" id="ajuda-link">❓ Ajuda</a>
            <a href="@Url.Action("Index", "Home")" id="logout-link" class="nav-link" style="margin-top: auto; color: #ff6b6b;">🚪 Sair</a>
        </div>

        <div class="main-content">
            <div class="dashboard-content">
                <h1>Bem-vindo(a) ao Zero Um Cursos!</h1>
                <p>Sua jornada de desenvolvimento tecnológico começa agora. Abaixo, seus cursos em andamento:</p>

                <div class="course-list">
                    <div class="course-card">
                        <h3>Introdução ao HTML5 e CSS3</h3>
                        <p>Progresso: 75% concluído</p>
                    </div>
                    <div class="course-card">
                        <h3>JavaScript Essencial para Web</h3>
                        <p>Progresso: 40% concluído</p>
                    </div>
                    <div class="course-card">
                        <h3>Design Responsivo com Flexbox</h3>
                        <p>Progresso: 10% concluído</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal FAQ -->
    <div id="faq-modal" class="faq-modal">
        <div class="faq-content">
            <span id="close-faq" class="close-faq">&times;</span>
            <div id="faq-section"></div>
        </div>
    </div>

    <script src="~/js/FAQ.js"></script>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Código JS para logout
            const logoutLink = document.getElementById("logout-link");
            logoutLink.addEventListener("click", function (event) {
                event.preventDefault();
                if (confirm("Tem certeza de que deseja sair?")) {
                    window.location.href = this.href;
                }
            });
        });

        // Adiciona o código do menu hambúrguer
        document.addEventListener('DOMContentLoaded', function () {
            const menuToggle = document.getElementById('menu-toggle');
            const sidebar = document.querySelector('.sidebar');

            if (menuToggle && sidebar) {
                console.log("✅ Menu Hambúrguer e Sidebar encontrados!");

                menuToggle.addEventListener('click', function () {
                    console.log("Clique no Hambúrguer detectado!");
                    sidebar.classList.toggle('open');

                    if (sidebar.classList.contains('open')) {

                    } else {
                        menuToggle.textContent = '☰';
                        menuToggle.style.color = '#9c45e8';
                    }
                });
            }
        });
    </script>

</body>
</html>