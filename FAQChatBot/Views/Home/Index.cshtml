<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>Login - ZeroUm Cursos</title> 

    <!-- Link do CSS principal do site -->
    <link rel="stylesheet" href="~/css/styles.css">
</head>
<body>

    <!-- Container principal que segura toda a página -->
    <div class="container">

        <!-- Área do logo e slogan -->
        <div class="logo-text-area">
            <h1>Zero Um Cursos</h1> <!-- Título principal -->
            <img src="~/images/logo_zero_um.png" alt="Logo ZeroUm Cursos - Desenvolva com Tecnologia">
            <!-- Logo da empresa, alt é o texto alternativo caso a imagem não carregue -->
            <p>Desenvolva sua carreira em Tecnologia!</p> 
        </div>

        <!-- Área do formulário de login -->
        <div class="login-area">
            <div class="login-box">
                <h2>Acesse sua conta</h2> 

                <!-- FORMULÁRIO DE LOGIN -->
                <form asp-action="Login" method="post">
                    <!-- asp-action="Login" → envia os dados para a ação Login do HomeController -->
                    <!-- method="post" → garante que os dados serão enviados de forma segura (POST) -->

                    <!-- Grupo de input: E-mail -->
                    <div class="input-group">
                        <label for="email">E-mail</label> <!-- Texto explicativo -->
                        <input type="email" id="email" name="email" required placeholder="seu@email.com">
                        <!-- name="email" → precisa bater com o parâmetro string email da ação Login -->

                    </div>

                    <!-- Grupo de input: Senha -->
                    <div class="input-group">
                        <label for="senha">Senha</label>
                        <input type="password" id="senha" name="senha" required placeholder="********">
                        <!-- name="senha" → precisa bater com o parâmetro string senha da ação Login -->

                    </div>

                    <!-- Link para recuperar a senha -->
                    <a asp-controller="Home" asp-action="RecuperarSenha" class="forgot-password-link">
                        Esqueceu sua senha?
                    </a>

                    <!-- Botão para enviar o formulário -->
                    <button type="submit" class="btn-login">
                        Conecte-se
                        <!-- Ao clicar, envia o formulário via POST para a ação Login -->
                    </button>

                    <!-- Texto com link para cadastro de novos usuários -->
                    <p class="signup-text">
                        Ainda não tem conta?
                        <a asp-controller="Home" asp-action="Cadastro" class="btn-signup">
                            Cadastre-se
                        </a>
                    </p>

                    <!-- Mostrar mensagem de erro se houver no ViewData -->
                    @if (ViewData["Erro"] != null)
                    {
                        <p class="erro-login">@ViewData["Erro"]</p>
                        <!-- Mostra a mensagem de erro enviada pelo controller -->
                    }
                </form>
            </div>
        </div>
    </div>

    <!-- Script JavaScript do site -->
   
    <script src="~/js/script.js"></script>
</body>
</html>
